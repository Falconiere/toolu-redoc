{
  "$schema": "./../../scripts/guardrails/schema.json",
  "version": 2,
  "srcRoot": "src",
  "src": {
    "topLevel": ["app", "ui", "domains", "api", "utilities", "providers", "constants", "types"],
    "nested": {
      "domains/*": ["screens", "components", "hooks", "api", "__tests__"],
      "ui": ["theme", "__tests__"],
      "api": ["clients", "queries"]
    },
    "requireReadme": ["ui", "domains", "api", "utilities", "providers"]
  },
  "fileSize": {
    "max": 300,
    "skipExtensions": [".ts", ".tsx"]
  },
  "functionSize": {
    "max": 50,
    "overrides": {
      "**/*.tsx": 80
    }
  },
  "testDir": "__tests__",
  "testGlob": "*.test.ts *.test.tsx",
  "barrelNames": ["index.ts", "index.tsx"],
  "barrelExempt": ["src/app/**"],
  "bannedDeps": [
    "axios",
    "yup",
    "joi",
    "valibot",
    "superstruct",
    "ajv",
    "styled-components",
    "@emotion/react",
    "@emotion/styled"
  ],
  "requiredFiles": [
    {
      "path": "src/ui/globals.css",
      "why": "it is the only stylesheet and the only place Tailwind is imported — without it every utility class in the app is inert text"
    },
    {
      "path": "src/ui/theme/palette.css",
      "why": "the band seam: the --tone-* values every colour utility resolves through, and the four signal temperatures"
    },
    {
      "path": "src/ui/theme/scale.css",
      "why": "the fixed half of the language — spacing, radii, breakpoints, the type-* roles, motion; without it text-*, rounded-* and ease-* fall back to Tailwind's magic-number defaults"
    }
  ],
  "secrets": {
    "neverTracked": [".env"]
  },
  "shadowConfigs": [
    {
      "found": "lefthook.yaml",
      "use": "lefthook.yml",
      "why": "the lefthook 2.x installer shadows a .yaml, so the hooks never run"
    },
    {
      "found": "vitest.config.ts",
      "use": "vite.config.ts",
      "why": "a vitest config REPLACES vite.config.ts rather than merging, so the router plugin and the @/* alias vanish under test"
    }
  ],
  "ownedByLinter": ["folder-tree", "colocated-tests", "no-barrels", "filename-case", "patterns"]
}
