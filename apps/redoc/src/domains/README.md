# src/domains

Owns the domains surface. Keep this inventory current as files are added.

| Domain | Owns |
| --- | --- |
| `openapi/` | OpenAPI 3.0/3.1 paste/parse/load — `parseOpenApiDocument`, `loadOpenApiDocument`, `SpecLoadScreen` on `/`, nav model builders, fixtures under `__tests__/fixtures/` |
| `docs/` | Signal three-column docs shell (nav · main · rail), operation nav/selection UI on DTOs, temporary Petstore twin under `api/` |
