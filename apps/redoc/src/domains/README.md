# src/domains

Owns the domains surface. Keep this inventory current as files are added.

| Domain | Owns |
| --- | --- |
| `openapi/` | OpenAPI 3.0/3.1 paste/parse/load — `parseOpenApiDocument`, `loadOpenApiDocument`, `SpecLoadScreen` on `/`, fixtures under `__tests__/fixtures/` |
| `docs/` | Signal three-column docs shell (nav · main · rail) + temporary Petstore twin under `api/` |
