# src/ui

Owns the ui surface. Keep this inventory current as files are added.

| Path | Holds |
| --- | --- |
| `globals.css` | One stylesheet — imports palette + scale |
| `theme/` | `palette.css`, `scale.css`, `icons.ts` |
| `band-theme-toggle.tsx` | Light/Dark band control (Signal `.band-light`) |
