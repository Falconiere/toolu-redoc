# utilities/

Shared pure helpers used by the components in this package.

## What is here

- `class-names.ts` — joins conditional class strings, dropping anything falsy.

## What does NOT go here

- Components, or anything that renders JSX. Those belong in `components/`.
